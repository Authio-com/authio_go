name: ci
on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: authio-com/authio_ci/actions/setup-go@442429d80b3fb0a7ddab547856da3279b3729c61
        with:
          go-version: '1.25.11'
      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - name: govulncheck
        run: govulncheck ./...
      - run: go vet ./...
      - run: go test -race -count=1 ./...
      - run: go build ./...